# 09 进阶类型技巧

目标：探索映射类型、工具类型与高级推断。

常用工具类型：`Partial<T>`、`Required<T>`、`Pick<T,K>`、`Omit<T,K>`。

映射类型与索引签名：

```ts
type ReadOnly<T> = { readonly [K in keyof T]: T[K] }
```

条件类型与推断：

```ts
type ReturnType<T> = T extends (...args:any) => infer R ? R : any;
```

性能提示：复杂的条件/映射类型会增大 IDE 计算成本，慎用在热路径。
