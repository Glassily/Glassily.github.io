# 10 练习与示例

完成下列练习后可对照 `answers.md` 查看参考答案。

练习 1：实现 `deepFreeze<T>(obj: T): Readonly<T>`，使对象及其嵌套属性变为只读。

练习 2：写一个泛型函数 `pluck<T, K extends keyof T>(objs: T[], key: K): T[K][]`。

练习 3：声明一个库 `legacy-lib` 的类型文件并在 TS 项目中使用它。

练习 4：将一个小型 JS 项目迁移到 TS，列出遇到的类型问题并解决。
